{
  "name": "@vibecode-tours/cli-telemetry-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker accepting opt-in vibe-code-tours CLI telemetry events.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
